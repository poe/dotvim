Installation:

    git clone git://github.com/poe/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/dotfiles/vimrc ~/.vimrc
    ln -s ~/dotfiles/gvimrc ~/.gvimrc

Get submodules

    cd ~/.vim
    git submodule init
    git submodule update

